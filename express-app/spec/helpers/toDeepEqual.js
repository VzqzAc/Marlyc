const _ = require('underscore');

// Aparently jasmine's toEqual() makes a deep equality
// so this is unnecesary, I will leave it anyway as for further use


module.exports = {
  toDeepEqual = function (expected) {

  }
};
